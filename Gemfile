source 'https://rubygems.org'

group :jekyll_plugins do
  gem 'jekyll'
  gem 'jekyll-feed'
  gem 'jekyll-sitemap'
  gem 'jekyll-redirect-from'
  gem 'jemoji'
  gem 'webrick', '~> 1.8'
end

gem 'github-pages'
gem 'connection_pool', '2.5.0'

# Formerly default gems, removed from the standard library in Ruby 3.4+.
# Needed so Jekyll 3.9 can run on newer Ruby locally; harmless on GitHub Pages.
gem 'csv'
gem 'base64'
gem 'bigdecimal'
gem 'logger'
